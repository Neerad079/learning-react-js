# React Learning Journey

Practice repo documenting my React.js learning

## Topics Covered

- Components & JSX
- Props & data flow
- Conditional rendering
- List rendering (map, keys)
- - Click events
- React Hooks
  - `useState`
- `onChange` event handler
- Controlled inputs
- Color picker using `useState` and `onChange`
- Counter using `useState`

## Structure

Each folder corresponds to a topic, with small standalone examples.
